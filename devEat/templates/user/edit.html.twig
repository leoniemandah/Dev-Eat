{% extends 'base.html.twig' %}

{% block title %}Modifier utilisateur
{% endblock %}

{% block body %}
	<h1 class="text-center mt-5 mb-5">Modifier le profil</h1>

	{{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}
	{% if is_granted('ROLE_ADMIN')  %}

		<div style="display: flex;justify-content: space-around;">
			<a class="btn mb-3 mt-3" style="background-color: #585455;color: white;" href="{{ path('admin_users') }}">Retour à la liste</a>

		{% endif %}
		{{ include('user/_delete_form.html.twig') }}
	</div>
{% endblock %}
